class Solution:
    def isOneBitCharacter(self, bits):
        """
        :type bits: List[int]
        :rtype: bool
        """
        bits.pop()
        oneCount = bits.count(1)
        zeroCount = bits.count(0)




print(t[: t.index(s)] + t[t.index(s) + len(s):])
